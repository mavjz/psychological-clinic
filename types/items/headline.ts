 export type HeadlineType = {
    h1?: boolean,
    h2?: boolean,
    h3?: boolean,
    h4?: boolean,
    h5?: boolean,
    h6?: boolean,
    text: string,
    place?: string,
    color?: string,
    isUppercase?: boolean
}